{% extends 'base.html.twig' %}
{% block body %}
<h2>{{ item.id ? 'Modifier réunion' : 'Nouvelle réunion' }}</h2>
{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn">Enregistrer</button>
{{ form_end(form) }}
{% endblock %}
